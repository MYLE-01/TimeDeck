# TimeDeck ⏱️📅

TimeDeck is a lightweight roster and shift management tool built for crews who need clarity and flexibility.  
It lets you edit hours, add extra entries, and even generate Google Calendar QR codes for quick scheduling.

## ✨ Features

- Edit Day/Night/Off shifts with a clean modal UI
- Add extra entries with notes
- Generate QR codes to add shifts directly to Google Calendar
- Track totals: done, left, and covers

## 🚀 Getting Started

Clone the repo:

```bash
git clone https://github.com/MYLE-01/TimeDeck.git
cd TimeDeck

---

### 2. **Issues & Projects**
- Use GitHub Issues to track bugs (like the `removeBtn` scope bug).
- Create a Project board for features/refactors (e.g., “Modal Refactor,” “QR UX polish”).

---

### 3. **Branching Strategy**
- Keep `main` clean and stable.
- Use feature branches:
  `feature/qr-toggle-refactor`
  `bugfix/removeBtn-scope`

---

### 4. **.gitignore**
Make sure you’re not committing generated files or secrets. Example:
```
